{
  "name": "x-timeago",
  "version": "1.2.0",
  "description": "Simple web component that displays dates as relative timestamps (e.g. \"4 minutes ago\").",
  "type": "module",
  "main": "dist/timeago.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/timeago.js && esbuild dist/timeago.js --minify --outfile=dist/timeago.min.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davidhirtz/timeago-web-component.git"
  },
  "devDependencies": {
    "date-fns": "^4.1.0",
    "esbuild": "^0.25"
  }
}
