{
  "name": "x-timeago",
  "version": "1.0.0",
  "description": "Simple web component that displays relative timestamps (e.g. \"4 minutes ago\") for dates.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --outfile=dist/index.js && esbuild dist/index.js --minify --outfile=dist/index.min.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/davidhirtz/timago.git"
  },
  "devDependencies": {
    "date-fns": "^4.1.0",
    "esbuild": "^0.24.2"
  }
}
